# Modal credentials (set via `uv run modal token set`)
# Stored in ~/.modal.toml, not needed here

# HuggingFace token (stored as Modal secret, not needed locally)
# Create with: uv run modal secret create huggingface-secret HF_TOKEN=hf_xxx --force
HF_TOKEN=hf_your_token_here
